
def GIT_CREDENTIALS="mycredentials"
pipeline{
    agent any
    stages{
            stage('checkout code'){
                steps{
                    script {
                    git credentialID: GIT_CREDENTIALS
                    sh 'git clone https://github.com/ponnamanda-srinu/office-document.git'
                    } 
                }
        }
    }
}
