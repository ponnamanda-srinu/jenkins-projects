
def GIT_CREDENTIALS="ponnamanda-srinu"
pipeline{
    agent any
    stages{
            stage('checkout code'){
                steps{
                    script {
                    sh 'git clone https://github.com/ponnamanda-srinu/jenkins-projects.git'
                    git credentialID: GIT_CREDENTIALS
                    } 
                }
        }
    }
}
