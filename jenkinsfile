
def GIT_CREDENTIALS="mycredentials"
pipeline{
    agent any
    stages{
            stage('checkout code'){
                steps{
                    script {
                    sh 'git clone https://github.com/office-document.git'
                    git credentialID: GIT_CREDENTIALS
                    } 
                }
        }
    }
}
