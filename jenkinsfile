
def GIT_CREDENTIALS="mycredentials"
pipeline{
    agent any
    stages{
            stage('checkout code'){
                steps{
                    script {
                    sh 'git clone https://github.com/ponnamanda-srinu/office-document'
                    git credentialID: GIT_CREDENTIALS
                    } 
                }
        }
    }
}
